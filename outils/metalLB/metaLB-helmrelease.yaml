apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metallb
  namespace: outils
spec:
  releaseName: metallb
  targetNamespace: outils
  interval: 5m
  chart:
    spec:
      chart: metallb
      version: "0.14.*"
      sourceRef:
        kind: HelmRepository
        name: metallb
        namespace: outils
  install:
    createNamespace: true
